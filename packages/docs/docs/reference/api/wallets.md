# api/wallets

## fetchActiveWallets()

> **fetchActiveWallets**(): `Promise`\<`ActiveWallets`\>

Fetches the active wallets

### Returns

`Promise`\<`ActiveWallets`\>

### Source

packages/sdk/src/api/wallets.ts:7
